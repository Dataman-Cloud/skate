server:
  port: 8761
spring:
  application:
    name: discovery-service
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: false
logging:
  level:
    demo: info

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://${DOCKER_IP:192.168.31.46}:8888
