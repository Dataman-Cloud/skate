server:
  port: 0
spring:
  application:
    name: payment-service
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://payment-service:8888
---
spring:
  profiles: default
  cloud:
    config:
      uri: http://localhost:8888
spring.profiles.active: development
---
spring:
  profiles: cloud
  cloud:
    config:
      uri: ${vcap.services.config-service.credentials.uri:http://localhost:8888}